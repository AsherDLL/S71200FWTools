#!/usr/bin/env python3
"""Run s7fw without installing it (adds this directory to sys.path)."""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from s7fw.cli import main

if __name__ == "__main__":
    sys.exit(main())
